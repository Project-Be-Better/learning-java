package com.car;

public class CarService {

    public int registerCar(Car car) {

        // check if the car is null
        // check if the reg number is valid
        // check if the reg number is taken
        // if price is <0
        return 1;
    };
}
